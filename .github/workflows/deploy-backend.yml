name: 🚀 Deploy Backend to AWS Lambda

on:
  push:
    branches: [main]
    paths:
      - 'apps/backend/**'
      - 'packages/types/**'
      - 'deploy/deploy-backend.sh'

  workflow_dispatch:
    inputs:
      stage:
        description: 'Deployment stage'
        required: true
        default: 'prod'
        type: choice
        options: [prod, staging]

env:
  AWS_REGION: ap-southeast-1
  ECR_REPOSITORY: ai-interview-backend
  STAGE: ${{ github.event.inputs.stage || 'prod' }}

jobs:
  deploy-backend:
    name: Build & Deploy Backend Lambda
    environment: prod
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify AWS secrets present
        run: |
          ERR=0
          if [ -z "${{ secrets.AWS_ACCESS_KEY_ID }}" ]; then
            echo "::error::GitHub Secret 'AWS_ACCESS_KEY_ID' is missing or empty."
            ERR=1
          fi
          if [ -z "${{ secrets.AWS_SECRET_ACCESS_KEY }}" ]; then
            echo "::error::GitHub Secret 'AWS_SECRET_ACCESS_KEY' is missing or empty."
            ERR=1
          fi
          if [ $ERR -eq 1 ]; then
            echo "Please add AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY under Repository Secrets."
            exit 1
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set image metadata
        id: meta
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          ECR_REGISTRY=${{ steps.login-ecr.outputs.registry }}
          IMAGE_URI="${ECR_REGISTRY}/${{ env.ECR_REPOSITORY }}:${{ env.STAGE }}-${SHORT_SHA}"
          echo "image-uri=${IMAGE_URI}" >> "$GITHUB_OUTPUT"
          echo "short-sha=${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Create ECR repository (if not exists)
        run: |
          aws ecr describe-repositories --repository-names ${{ env.ECR_REPOSITORY }} --region ${{ env.AWS_REGION }} \
            || aws ecr create-repository \
                --repository-name ${{ env.ECR_REPOSITORY }} \
                --region ${{ env.AWS_REGION }} \
                --image-scanning-configuration scanOnPush=true

      - name: Build Docker image
        run: |
          docker build \
            --platform linux/amd64 \
            -f apps/backend/Dockerfile.lambda \
            -t ${{ steps.meta.outputs.image-uri }} \
            .

      - name: Push image to ECR
        run: |
          docker push ${{ steps.meta.outputs.image-uri }}
          REGISTRY=${{ steps.login-ecr.outputs.registry }}
          docker tag ${{ steps.meta.outputs.image-uri }} \
            "${REGISTRY}/${{ env.ECR_REPOSITORY }}:${{ env.STAGE }}-latest"
          docker push "${REGISTRY}/${{ env.ECR_REPOSITORY }}:${{ env.STAGE }}-latest"

      - name: Install Serverless Framework v3
        run: npm install -g serverless@3

      - name: Verify Backend Environment Secrets
        run: |
          ERR=0
          if [ -z "${{ secrets.DATABASE_URL }}" ]; then echo "::error::Secret 'DATABASE_URL' is missing."; ERR=1; fi
          if [ -z "${{ secrets.DIRECT_URL }}" ]; then echo "::error::Secret 'DIRECT_URL' is missing."; ERR=1; fi
          if [ -z "${{ secrets.JWT_SECRET }}" ]; then echo "::error::Secret 'JWT_SECRET' is missing."; ERR=1; fi
          if [ -z "${{ secrets.JWT_REFRESH_SECRET }}" ]; then echo "::error::Secret 'JWT_REFRESH_SECRET' is missing."; ERR=1; fi
          if [ -z "${{ secrets.GEMINI_API_KEY }}" ]; then echo "::error::Secret 'GEMINI_API_KEY' is missing."; ERR=1; fi
          if [ $ERR -eq 1 ]; then
            echo "One or more required backend secrets are missing in GitHub Repository Secrets!"
            exit 1
          fi

      - name: Deploy via Serverless Framework
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          JWT_REFRESH_SECRET: ${{ secrets.JWT_REFRESH_SECRET }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CORS_ORIGINS: ${{ secrets.CORS_ORIGINS }}
          ECR_IMAGE_URI: ${{ steps.meta.outputs.image-uri }}
        run: |
          cd apps/backend
          serverless deploy \
            --stage ${{ env.STAGE }} \
            --region ${{ env.AWS_REGION }} \
            --verbose

      - name: Setup pnpm & Node for Prisma Migration
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install Monorepo Dependencies
        run: pnpm install --frozen-lockfile

      - name: Run DB Migrations
        env:
          DIRECT_URL: ${{ secrets.DIRECT_URL }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          cd apps/backend
          npx prisma migrate deploy

      - name: Notify deployment success
        run: |
          echo "## ✅ Backend Deployed Successfully!" >> $GITHUB_STEP_SUMMARY
          echo "- **Stage:** ${{ env.STAGE }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Image:** \`${{ steps.meta.outputs.image-uri }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ steps.meta.outputs.short-sha }}" >> $GITHUB_STEP_SUMMARY
